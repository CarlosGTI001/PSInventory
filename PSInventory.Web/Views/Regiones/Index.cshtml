@model IEnumerable<PSData.Modelos.Region>

@{
    ViewData["Title"] = "Regiones";
    var totalRegiones = ViewBag.TotalCount != null ? (int)ViewBag.TotalCount : Model.Count();
    var conDescripcion = ViewBag.ConDescripcion != null ? (int)ViewBag.ConDescripcion : Model.Count(r => !string.IsNullOrEmpty(r.Descripcion));
    var activas = ViewBag.Activas != null ? (int)ViewBag.Activas : Model.Count(r => r.Activo);
    var q = ViewBag.Query as string ?? "";
    var activo = ViewBag.ActivoFiltro as string ?? "";
    var currentPage = ViewBag.Page != null ? (int)ViewBag.Page : 1;
    var totalPages = ViewBag.TotalPages != null ? (int)ViewBag.TotalPages : 1;
    var pageSize = ViewBag.PageSize != null ? (int)ViewBag.PageSize : 30;
}

<!-- Breadcrumb -->
<div style="margin-bottom: 16px; display: flex; align-items: center; gap: 8px; color: var(--md-sys-color-on-surface-variant); font-size: 14px;">
    <md-icon style="font-size: 18px;">home</md-icon>
    <span>Configuración</span>
    <md-icon style="font-size: 18px;">chevron_right</md-icon>
    <span style="color: var(--md-sys-color-primary);">Regiones</span>
</div>

<!-- Page Header -->
<div class="page-header">
    <div>
        <h1 class="md-typescale-display-small" style="margin: 0 0 8px 0;">Regiones Geográficas</h1>
        <p class="md-typescale-body-large" style="color: var(--md-sys-color-on-surface-variant); margin: 0;">
            Administra las regiones donde se ubican las sucursales
        </p>
    </div>
    <md-filled-button onclick="window.location.href='@Url.Action("Create")'">
        <md-icon slot="icon">add</md-icon>
        Nueva Región
    </md-filled-button>
</div>

<!-- Stats Cards -->
<div class="stats-grid" style="margin-bottom: 32px;">
    <div class="stat-card">
        <div class="stat-card-header">
            <div>
                <div class="stat-card-value">@totalRegiones</div>
                <div class="stat-card-label">Total Regiones</div>
            </div>
            <div class="stat-card-icon">
                <md-icon>public</md-icon>
            </div>
        </div>
    </div>
    
    <div class="stat-card">
        <div class="stat-card-header">
            <div>
                <div class="stat-card-value" style="color: var(--md-sys-color-success);">@conDescripcion</div>
                <div class="stat-card-label">Con Descripción</div>
            </div>
            <div class="stat-card-icon" style="background: rgba(76, 175, 80, 0.15);">
                <md-icon style="color: var(--md-sys-color-success);">description</md-icon>
            </div>
        </div>
    </div>
    
    <div class="stat-card">
        <div class="stat-card-header">
            <div>
                <div class="stat-card-value" style="color: var(--md-sys-color-secondary);">@activas</div>
                <div class="stat-card-label">Activas</div>
            </div>
            <div class="stat-card-icon" style="background: rgba(255, 92, 0, 0.15);">
                <md-icon style="color: var(--md-sys-color-secondary);">location_on</md-icon>
            </div>
        </div>
    </div>
    
    <div class="stat-card">
        <div class="stat-card-header">
            <div>
                <div class="stat-card-value" style="color: var(--md-sys-color-primary);">@(totalRegiones == 0 ? "0%" : $"{(activas * 100.0 / totalRegiones):F0}%")</div>
                <div class="stat-card-label">Cobertura</div>
            </div>
            <div class="stat-card-icon" style="background: rgba(4, 115, 148, 0.15);">
                <md-icon style="color: var(--md-sys-color-primary);">map</md-icon>
            </div>
        </div>
    </div>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        <md-icon>check_circle</md-icon>
        <span>@TempData["Success"]</span>
    </div>
}

<!-- Data Table -->
<div class="card">
    <div style="display: flex; align-items: center; justify-content: space-between; margin-bottom: 20px;">
        <div style="display: flex; align-items: center; gap: 12px;">
            <md-icon style="color: var(--md-sys-color-primary);">table_chart</md-icon>
            <h3 class="md-typescale-title-medium" style="margin: 0;">Lista de Regiones</h3>
        </div>
        <span class="md-typescale-body-medium" style="color: var(--md-sys-color-on-surface-variant);">
            @totalRegiones región@(totalRegiones != 1 ? "es" : "") registrada@(totalRegiones != 1 ? "s" : "")
        </span>
    </div>
    <form method="get" action="@Url.Action("Index")" style="display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:12px; margin-bottom:16px;">
        <md-filled-text-field name="q" label="Buscar región" value="@q" style="min-width:280px;">
            <md-icon slot="leading-icon">search</md-icon>
        </md-filled-text-field>
        <div class="md3-select-field has-leading-icon">
            <md-icon class="md3-select-leading-icon">check_circle</md-icon>
            <select name="activo" class="md3-select">
                <option value="" selected="@(string.IsNullOrEmpty(activo))">Todos</option>
                <option value="1" selected="@(activo == "1")">Activas</option>
                <option value="0" selected="@(activo == "0")">Inactivas</option>
            </select>
            <label class="md3-select-label">Estado</label>
        </div>
        <div class="md3-select-field has-leading-icon">
            <md-icon class="md3-select-leading-icon">format_list_numbered</md-icon>
            <select name="pageSize" class="md3-select">
                <option value="10" selected="@(pageSize == 10)">10 por página</option>
                <option value="30" selected="@(pageSize == 30)">30 por página</option>
                <option value="50" selected="@(pageSize == 50)">50 por página</option>
                <option value="100" selected="@(pageSize == 100)">100 por página</option>
            </select>
            <label class="md3-select-label">Paginación</label>
        </div>
        <input type="hidden" name="page" value="1" />
        <div style="display:flex; gap:8px; align-items:end;">
            <md-filled-button type="submit"><md-icon slot="icon">search</md-icon>Filtrar</md-filled-button>
            <md-outlined-button type="button" onclick="window.location.href='@Url.Action("ExportarCsv", new { q, activo })'"><md-icon slot="icon">download</md-icon>Exportar CSV</md-outlined-button>
            <md-text-button onclick="window.location.href='@Url.Action("Index")'; return false;">Limpiar</md-text-button>
        </div>
    </form>

    <div class="data-table-container">
        <table class="data-table">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th class="actions-column">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <div style="display: flex; align-items: center; gap: 8px;">
                                <md-icon style="color: var(--md-sys-color-secondary); font-size: 20px;">place</md-icon>
                                <strong>@item.Nombre</strong>
                            </div>
                        </td>
                        <td>@item.Descripcion</td>
                        <td class="actions-cell">
                            <md-icon-button title="Ver Infraestructura" onclick="window.location.href='@Url.Action("Index", "Infraestructura", new { regionId = item.RegionId })'">
                                <md-icon>devices</md-icon>
                            </md-icon-button>
                            <md-icon-button onclick="window.location.href='@Url.Action("Edit", new { id = item.RegionId })'">
                                <md-icon>edit</md-icon>
                            </md-icon-button>
                            <md-icon-button onclick="confirmDelete(@item.RegionId, '@item.Nombre')">
                                <md-icon>delete</md-icon>
                            </md-icon-button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    @if (totalPages > 1)
    {
        <div style="display:flex; gap:12px; justify-content:flex-end; align-items:center; margin-top:16px;">
            <span class="md-typescale-body-small">Página @currentPage de @totalPages</span>
            <md-outlined-button onclick="window.location.href='@Url.Action("Index", new { q, activo, page = currentPage - 1, pageSize })'" @(currentPage <= 1 ? "disabled" : "")>Anterior</md-outlined-button>
            <md-outlined-button onclick="window.location.href='@Url.Action("Index", new { q, activo, page = currentPage + 1, pageSize })'" @(currentPage >= totalPages ? "disabled" : "")>Siguiente</md-outlined-button>
        </div>
    }
</div>

@if (!Model.Any())
{
    <div class="empty-state">
        <md-icon style="font-size: 64px; color: var(--md-sys-color-outline);">location_on</md-icon>
        <h3 class="md-typescale-headline-small">No hay regiones registradas</h3>
        <p class="md-typescale-body-medium">Comienza creando tu primera región</p>
        <md-filled-button onclick="window.location.href='@Url.Action("Create")'">
            <md-icon slot="icon">add</md-icon>
            Crear Región
        </md-filled-button>
    </div>
}

@section Scripts {
    <script>
        async function confirmDelete(id, nombre) {
            if (await showConfirmDialog(
                'Eliminar Región',
                `¿Estás seguro de eliminar la región "${nombre}"?`,
                'Eliminar'
            )) {
                const formData = new FormData();
                formData.append('__RequestVerificationToken', document.querySelector('input[name="__RequestVerificationToken"]').value);
                
                const response = await fetch(`@Url.Action("Delete")/` + id, {
                    method: 'POST',
                    body: formData
                });
                
                const result = await response.json();
                
                if (result.success) {
                    showSnackbar(result.message, 'success');
                    setTimeout(() => location.reload(), 1500);
                } else {
                    showSnackbar(result.message, 'error');
                }
            }
        }
    </script>
    @Html.AntiForgeryToken()
}
